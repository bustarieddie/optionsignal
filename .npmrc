; The Sneat template ships a dev dependency (eslint-config-airbnb-base) with a
; peer-dependency conflict. Allow installs to resolve it automatically so CI /
; Forge / fresh `npm install` don't fail.
legacy-peer-deps=true
